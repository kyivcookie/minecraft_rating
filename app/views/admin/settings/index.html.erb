<div class="border-form-div" style="max-width: 550px;">

<% settings = Settings.all %>
<%= bootstrap_form_for 'settings', url: admin_settings_path do |f| %>
    <% settings.each do |s| %>
        <%= f.text_field s[:key], value: s[:value] %>
    <% end %>
    <%= f.submit %>
<% end %>

</div>